# 全库重复数据审计报告

## ts.kline

- 口径: `symbol, freq, adj_type, date(time at time zone 'Asia/Shanghai')`
- 来源: `spec:kline_trade_day`
- 动作: `manual_review`
- 重复组: `0`
- 重复行: `0`
- 多余行: `0`
- 错误: `DBAPIError: (sqlalchemy.dialects.postgresql.asyncpg.Error) <class 'asyncpg.exceptions.QueryCanceledError'>: canceling statement due to statement timeout
[SQL: 
        WITH dup AS (
          SELECT symbol, freq, adj_type, date(time at time zone 'Asia/Shanghai'), COUNT(*) AS rows
          FROM "kline"
          WHERE freq in ('1d', '1w', '1M')
          GROUP BY symbol, freq, adj_type, date(time at time zone 'Asia/Shanghai')
          HAVING COUNT(*) > 1
        )
        SELECT
          COUNT(*) AS duplicate_groups,
          COALESCE(SUM(rows), 0) AS duplicate_rows,
          COALESCE(SUM(rows - 1), 0) AS extra_rows
        FROM dup
    ]
(Background on this error at: https://sqlalche.me/e/20/dbapi)`
